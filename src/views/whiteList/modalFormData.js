// 自定义模板
export const customModalForm = {
  template_type: 0 // 模板类型
}

export const customModalFormRules = {
  template_type: [
    { required: true, message: '请选择模板类型' }
  ]
}

// 店铺模板
export const linkModalForm = {
  template_type: 0 // 模板类型
}

export const linkModalFormRules = {
  template_type: [
    { required: true, message: '请选择模板类型' }
  ]
}

// 店铺模板
export const shopModalForm = {
  template_type: 0 // 模板类型
}

export const shopModalFormRules = {
  template_type: [
    { required: true, message: '请选择模板类型' }
  ]
}
